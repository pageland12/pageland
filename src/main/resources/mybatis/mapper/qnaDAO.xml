<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.pageland.dao.IQnaDAO">
	<select id="qnaList" resultType="com.springboot.pageland.dto.QnaDTO">
		select * from qna order by qno desc
	</select>
	<select id="qnaView" resultType="com.springboot.pageland.dto.QnaDTO">
		select * from qna where qno = #{qno}
	</select>
	<insert id="qnaWrite">
		insert into qna(qno, qtitle, qcontent, qfiles, qpasswd, qsecret, qdate, mno)
		 values(QNA_SEQ.NEXTVAL, #{qtitle}, #{qcontent}, #{qfiles}, #{qpasswd}, #{qsecret}, SYSDATE, #{mno})
	</insert>
	<update id="qnaUpdate">
		update qna
		 set qcontent = #{qcontent}, qfiles = #{qfiles}, qsecret = #{qsecret}
		 where qno = #{qno}
	</update>
	<delete id="qnaDelete">
		delete from qna
		 where qno = #{qno}
	</delete>
</mapper>
